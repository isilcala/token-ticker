# Provider Notes

Current provider notes:

- `minimax.md` - direct quota polling notes for MiniMax CN and global regions

Future provider notes should capture:

- public endpoint or SDK evidence
- auth method
- response contract
- refresh and rate-limit constraints
- whether standalone device support is realistic without a desktop relay
