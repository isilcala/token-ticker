# =============================================================================
# ESP-IDF build output (regenerated by `idf.py build` / `idf.py test`)
# =============================================================================
firmware/build/
firmware/test/build/
firmware/sdkconfig.old
firmware/test/sdkconfig

# ESP-IDF component manager output (regenerated from dependencies.lock)
firmware/managed_components/
firmware/test/managed_components/

# =============================================================================
# ESP-IDF / GCC build artifacts (defense in depth if anything escapes build/)
# =============================================================================
*.elf
*.bin
*.map
*.hex
*.uf2
*.o
*.obj
*.a
*.lib
*.dep
*.d

# CMake / Ninja scratch
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
build.ninja
.ninja_deps
.ninja_log
compile_commands.json

# =============================================================================
# Secrets / local overrides — never commit
# =============================================================================
secrets.*
*.env
*.env.*
credentials.*
*.key
*.pem
api_token*
token*

# =============================================================================
# Large vendor zip files — exceeds GitHub's 100 MB file limit
# Re-download from Waveshare if needed.
# =============================================================================
external/vendor/waveshare/hardware/ESP32-S3-RLCD-4.2-Demo.zip

# =============================================================================
# Vendored upstream repos — cloned as research material, each has its own .git
# Re-clone via the helper script if needed; do NOT track them here.
# `external/vendor/` (PDFs, datasheets, doc snapshots) IS tracked.
# =============================================================================
external/repos/

# =============================================================================
# IDE / editor scratch
# =============================================================================
.vscode/.browse.VC.db
.vscode/.vscode/
.vscode/ipch/
.idea/
*.swp
*.swo
*~
*.bak
*.orig
*.rej
*.tmp
.cache/
.ccache/

# =============================================================================
# OS noise
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# =============================================================================
# Python tooling (if any helper scripts are added later)
# =============================================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# =============================================================================
# Logs
# =============================================================================
*.log
logs/

# =============================================================================
# Local notes — keep `docs/` tracked, but allow per-user scratch notes
# =============================================================================
TODO.local.md
NOTES.local.md